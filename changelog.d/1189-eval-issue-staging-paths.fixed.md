Eval artifact compile and CI issue evidence now replaces each per-validation
temporary RuleSpec staging root with `<rulespec-validation-root>`, keeping exact
issue comparisons location-independent without dropping tamper-sensitive fields.
