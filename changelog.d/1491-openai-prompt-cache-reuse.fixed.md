GPT-5.6 OpenAI-backend generations now mark the exact reusable RuleSpec prompt prefix with an explicit cache breakpoint and route it with a model- and schema-bound `prompt_cache_key`; older OpenAI models retain their string input while sharing the same retry-stable key identity. To make that prefix reusable, retry-only validation feedback moves from its former mid-prompt position to immediately before the rejected candidate near the tail; no source, instruction, feedback, candidate, or output-contract text is removed or rewritten. Ordered content blocks preserve the newly assembled prompt byte-for-byte, and the existing compile, CI, grounding, and validation gates remain unchanged. GPT-5.6 requests explicitly use Standard processing, and per-attempt logs now report both `cache_read=` and `cache_write=` so measured savings include cache-write charges; per-attempt and aggregate retry estimates fail closed outside the configured short-context pricing tier (axiom-encode#1491).
