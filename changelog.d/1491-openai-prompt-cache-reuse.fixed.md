GPT-5.6 OpenAI-backend generations now mark the exact reusable RuleSpec prompt prefix with an explicit cache breakpoint and route it with a model- and schema-bound `prompt_cache_key`. Retry-only validation feedback and rejected candidate bytes remain after that boundary, while ordered content blocks reassemble to the prior model-visible prompt byte-for-byte. Per-attempt logs now report both `cache_read=` and `cache_write=` so measured savings include GPT-5.6 cache-write charges (axiom-encode#1491).
