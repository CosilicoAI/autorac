GPT-5.6 OpenAI-backend generations now mark the exact reusable RuleSpec prompt prefix with an explicit cache breakpoint and route it with a model- and schema-bound `prompt_cache_key`; older OpenAI models retain their string input while sharing the same retry-stable key identity. Retry-only validation feedback and rejected candidate bytes remain after that boundary, while ordered content blocks reassemble to the prior model-visible prompt byte-for-byte. GPT-5.6 requests explicitly use Standard processing, and per-attempt logs now report both `cache_read=` and `cache_write=` so measured savings include cache-write charges; estimates fail closed outside the configured short-context pricing tier (axiom-encode#1491).
