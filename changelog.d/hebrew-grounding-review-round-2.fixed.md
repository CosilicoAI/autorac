Refuse to rewrite a generated RuleSpec artifact that is a symlink or resolves outside the generation's output root, and prove that before the unescape pass writes anything. Claim the flattened mixed-number reading of a glued fraction slash only where the flattening happened (a non-zero proper fraction inside Hebrew prose), consume the run so the concatenated digits never surface as a grounded literal, and read Hebrew teen numerals joined by a hyphen or a maqaf.
