Add an unsigned, provisional `notary-verify` receipt profile that derives the complete protected target set from a full ancestor `base_commit`, records verified/deleted/out-of-scope dispositions, runs deterministic gates plus pinned oracles without reviewer or model calls, requires a verification-only corpus broker, and fails closed when pinned oracle evidence is unavailable unless reduced evidence is explicitly allowed.
