Add an unsigned, provisional `notary-verify` receipt profile that derives the complete protected target set from a trusted-workflow-bound ancestor `base_commit`, records verified/deleted/out-of-scope dispositions, and rejects empty diffs plus authority-surface and privileged-mode changes. Backfill-only whole-repository receipts explicitly carry a null base and `whole-repo` mode, never authorize a diff, inventory legitimate authority surfaces (including encoding manifests under any exact `.axiom` path component), and fail closed only on symlinks or non-regular/privileged modes inside protected content trees before verifying every protected module. The profile runs deterministic gates plus pinned oracles without reviewer or model calls, requires a verification-only corpus broker, and publishes only to a pre-existing trusted external directory after race-resistant input identity checks; failed cleanup of a rejected publication now emits a warning instead of being silently suppressed. Pinned oracle evidence fails closed unless reduced evidence is explicitly allowed.
