Add an unsigned, provisional `notary-verify` receipt profile that derives the complete protected target set from a trusted-workflow-bound ancestor `base_commit`, records verified/deleted/out-of-scope dispositions, and rejects empty diffs plus authority-surface and privileged-mode changes. Backfill-only whole-repository receipts explicitly carry a null base and `whole-repo` mode, never authorize a diff, inventory legitimate authority surfaces, and fail closed only on symlinks or non-regular/privileged modes inside protected content trees before verifying every protected module, including tracked jurisdiction `manual/**` RuleSpec modules. Every file under an exact `.axiom` path component is authority surface, including `.gitignore`; ordinary `.gitignore` files elsewhere remain non-authority. The profile runs deterministic gates plus pinned oracles without reviewer or model calls, requires a verification-only corpus broker, and publishes only to a pre-existing trusted external directory after race-resistant input identity checks. Rejected-publication cleanup and receipt-parent close failures preserve the primary error and emit warnings. Git identity probes use a documented 10-second minimum that `AXIOM_ENCODE_GIT_PROBE_TIMEOUT_SECONDS` may raise, while PolicyEngine oracle subprocesses retain fixed 300-second wall and 45-second idle limits with truthful timeout diagnostics. Pinned oracle evidence fails closed unless reduced evidence is explicitly allowed.
